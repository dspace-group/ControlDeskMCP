"""MCP resources — read-only data exposed to the LLM and MCP Inspector.

Importing this package's sub-modules registers all @mcp.resource decorators.
Add new resource modules to sources/server/registry.py to activate them.
"""
